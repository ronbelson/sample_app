<% @user ||= current_user %>
 <div>
	<table>
		<tr>
			<td>
				<a href="<%= following_user_path(@user) %>"><%= @user.following.count  %> followed</a>
			</td>
			<td>
				<div id="followers">
				<a href="<%= followers_user_path(@user) %>"><%= @user.followers.count %> followers</a>
				<div>
			</td>
		</tr>
   </table>
<div>